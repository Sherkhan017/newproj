CREATE table Profile (
                     profileID int PRIMARY KEY ,
                    Age int NOT NULL,
                     Username varchar(255),
                     Profileinfo varchar(255)
)
